package spaceimpact.view;

import javafx.application.Application;
import javafx.scene.canvas.Canvas;
import javafx.scene.canvas.GraphicsContext;
import javafx.scene.image.Image;
import javafx.scene.paint.Color;
import javafx.stage.Stage;
import spaceimpact.utilities.ImageLoader;

/**
 * Class showing the main logo of space invaders game.
 * 
 * @author Tommaso Bonato
 */
/*public class Logo extends Application {
        
        private static final String LOGO_ICON_PATH = "/res/icon.jpg";
        private static final int X_POSITION_LOGO = 30;
        private static final int Y_POSITION_LOGO = 20;
        
        private Image logoImage;
        
        /**
         * Creates a new Logo Scene.
         *//*
        public Logo() {
                this.logoImage = ImageLoader.getLoader().getImageFromPath(LOGO_ICON_PATH);
        }
        
        @Override
        public void paintComponent(GraphicsContext g) {
                super.paintComponent(g);
                g.drawImage(logoImage, X_POSITION_LOGO, Y_POSITION_LOGO, null);
        }

        @Override
        public void start(Stage primaryStage) throws Exception {
            
        }
        

}
*/