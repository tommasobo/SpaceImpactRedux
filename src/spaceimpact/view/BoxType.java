package spaceimpact.view;

/**
 * An enum with all the possible types of Dialog Box.
 * 
 *
 */
public enum BoxType {
    /**
     * Currently there are two possible types of dialog box. The first one
     * (ERROR) to indicate an error. The second one (SUCCESS) to indicate that
     * the operation was successful
     */
    ERROR, SUCCESS;
}
